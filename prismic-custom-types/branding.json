{
  "Main": {
    "color_principal": {
      "type": "Color",
      "config": {
        "label": "Color principal (hexadecimal)"
      }
    },
    "color_menu": {
      "type": "Color",
      "config": {
        "label": "Color menú seleccionado (hexadecimal)"
      }
    },
    "fuente_principal": {
      "type": "Text",
      "config": {
        "label": "Fuente principal (Google Fonts)"
      }
    },
    "font_size_principal": {
      "type": "Text",
      "config": {
        "label": "Tamaño de fuente principal (px, ej: 18px)"
      }
    },
    "fuente_secundaria": {
      "type": "Text",
      "config": {
        "label": "Fuente secundaria (Google Fonts, opcional)",
        "placeholder": "Opcional"
      }
    },
    "font_size_secundaria": {
      "type": "Text",
      "config": {
        "label": "Tamaño de fuente secundaria (px, ej: 24px)"
      }
    },
    "fondo_color": {
      "type": "Color",
      "config": {
        "label": "Color de fondo (hexadecimal)"
      }
    },
    "fondo_imagen": {
      "type": "Image",
      "config": {
        "label": "Imagen de fondo (opcional)"
      }
    }
  }
} 